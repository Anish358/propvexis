/* Button — PropVexis primitive.
 *
 * Variants come from the generated component: default (brand fill), outline,
 * secondary, ghost, destructive, link. Sizes: xs, sm, default, lg, plus icon
 * variants. Every colour resolves through the token bridge — `bg-primary` is
 * var(--accent), `bg-destructive` is var(--loss).
 *
 * Re-export by design; see index.js. Give this a body when Button needs a
 * PropVexis default that the generated component should not carry. */
export { Button, buttonVariants } from "@/components/ui/button";
